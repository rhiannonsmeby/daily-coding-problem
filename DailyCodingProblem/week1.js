// Daily Coding Problems











// Day 4
// Asked by: Stripe
// [Hard]
// Given an array of integers, find the first missing positive integer in linear
// time and constant space. Find the lowest positive interger that does not 
// exist in the array. The array can contain duplicates and negative numbers as
// well.
// Input: [3, 4, -1, 1]
// Output: 2
// Input: [1, 2, 0]
// Output: 3

// Day 5
// Asked by: Jane Street
// cons(a, b) constructs a pair and car(pair) and cdr(pair) returns the first and
// last element of that pair e.g. car(cons(3, 4)) returns 3, and 
// cdr(cons(3, 4)) returns 4
// given this implementation of cons:


// Day 6
// Asked by: Google
// [Hard]
// An XOR linked list is a more memory efficient doubly linked list. Instead
// of each node holding next and prev fields, it holds a field named both, which
// is an XOR of the next node and the prev node. Implement an XOR linked list 
// it has an add(element) which adds the element to the end and a get(index)
// which returns the node at index

// Day 7
// Asked by: Facebook
// [Medium]
// Given the mapping a = 1, b = 2, ... z = 26, and an encoded message, count
// the number of ways it can be decoded
// e.g. the message '111' would give 3, since it could be decoded as 'aaa' 'ka'
// and 'ak'
// you can assume that the messages are decodable: '001' is not allowed



